Robot {
	void avancer(int x, int y){
	
	try{	
		currentState.avancer();
		Posiyion.setX(x);
		}catch(){

		}

	}
	void saisir(Obj o){
		saisir(Objet, this);
	}
};


current_state{
	saisir(Obj, Rob){
	}
	v_saisir(Obj, Rob){
		// vrai méthode
		Rob.setObjet(Obj);
	}
	avancer(int,int,Pos){}
};

aVideFacePlot{
	saisir(Obj, Rob){
		v_saisir(Obj, Rob);
	}
};

fige{};